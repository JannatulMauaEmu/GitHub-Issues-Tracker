1.var is function-scoped.let and const is bloked scoped.let er value can be changed but const remained unchanged.
it is used to marge multiple array
filter() & map () are used for createing new array but filter() is used for specific condition.foreach() is used for taking each function element to take as input.
